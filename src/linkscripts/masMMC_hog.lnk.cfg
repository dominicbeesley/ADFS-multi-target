MEMORY {
    ROM: 		    start = $8000, size = $4000, file = %O, fill=yes, fillval=$00;
}
SEGMENTS {
    rom_header:     	        load = ROM, type = ro;
    rom_main_1:                 load = ROM, type = ro;
    hd_driver_1:                load = ROM, type = ro;
    tube_claim:                 load = ROM, type = ro;
    hd_driver_2:                load = ROM, type = ro;
    tube_start_xfer:            load = ROM, type = ro;
    rom_main_2:                 load = ROM, type = ro;
    starMAP:                    load = ROM, type = ro;
    rom_main_3:                 load = ROM, type = ro;
    rom_main_4:                 load = ROM, type = ro;
    hd_driver_bget_bput:        load = ROM, type = ro;
    rom_main_5:                 load = ROM, type = ro;    
    hd_driver_bput:             load = ROM, type = ro;
    rom_main_6:                 load = ROM, type = ro;
    hd_driver_svc5:             load = ROM, type = ro;
    rom_main_7:                 load = ROM, type = ro;
    hd_driver_bget:             load = ROM, type = ro;
    rom_main_8:                 load = ROM, type = ro;
    rom_main_9:                 load = ROM, type = ro;
    mmc_driver_a:               load = ROM, type = ro;
    mmc_driver_b:               load = ROM, type = ro;
    rom_main_10:                load = ROM, type = ro;

}
FEATURES {
}
SYMBOLS {
}